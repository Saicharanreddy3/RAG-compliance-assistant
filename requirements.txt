# Core runtime -- everything needed to run the service offline.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0

# Optional document formats
pypdf>=4.0,<6.0

# Optional providers. Uncomment the ones you configure in .env; none are
# required for the default offline setup (EMBEDDING_PROVIDER=hash, LLM_PROVIDER=echo).
# boto3>=1.34            # EMBEDDING_PROVIDER/LLM_PROVIDER=bedrock
# openai>=1.30           # EMBEDDING_PROVIDER/LLM_PROVIDER=openai
# pinecone>=5.0          # VECTOR_BACKEND=pinecone
# mangum>=0.17           # AWS Lambda adapter behind API Gateway
